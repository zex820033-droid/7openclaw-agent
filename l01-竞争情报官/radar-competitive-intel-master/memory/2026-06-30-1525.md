# Session: 2026-06-30 15:25:08 GMT+8

- **Session Key**: agent:12_radar:dashboard:ae77e4b4-f0cf-4d95-abe6-a1781700d603
- **Session ID**: 7f7d2bbf-535b-4639-94e1-2b19181e0687
- **Source**: webchat

## Conversation Summary

assistant: 全貌已经清楚了。现在开始完整复制并适配。依赖链是：

```
works_collector.py → playwright_session.py → page_adapter.py + _stealth.py
                   → _utils.py → page_adapter.py + models
                   → models.py (DouyinContentItem/UserProfile/HotItem)

hotspot_collector.py → direct_crawler.py + hot_aggregator
                      → models.py
```
assistant: 先建核心模型文件：
assistant: 现在批量创建剩余文件。先做无依赖的叶子节点：
assistant: 现在适配 `_utils.py`（改 import 路径）：
assistant: 适配 `playwright_session.py`（改 2 行 import）：
assistant: 现在创建抖音采集器目录下的文件：
assistant: 创建简化版 `hotspot_collector.py`（去掉天行 API 和热榜聚合器依赖，保留核心的 60s API + 直连双层降级）：
assistant: 复制并适配核心的 `works_collector.py`：
assistant: 只是 docstring 里的历史示例，改掉即可：
assistant: 现在创建 `__init__.py` 文件并验证：
assistant: 创建 collectors 包的 `__init__.py`，然后做语法检查：
assistant: 全部 10 个文件语法通过。最后把 `data/direct-crawler-apis.json` 中的 douyin 条目也指向新采集器路径：
assistant: ---

## ✅ 适配完成 — 10 个文件，0 语法错误

### 📁 复制后的目录结构

```
12_radar/collectors/                    ← 新采集器根目录
├── __init__.py
├── _stealth.py                         ← 反检测 JS 注入（原封不动）
├── _utils.py                           ← 工具函数（改2行import）
├── page_adapter.py                     ← Playwright/Selenium 统一接口（原封不动）
├── playwright_session.py              ← 浏览器会话管理（改3行import）
│
└── douyin/
    ├── __init__.py                     ← 导出汇聚
    ├── models.py                       ← 数据模型（全新提取，从 super_aigc_collectors）
    ├── direct_crawler.py              ← 热搜直连 API（原封不动，仅 requests）
    ├── hotspot_collector.py           ← 热搜双降级（60s API→直连，去掉天行/聚合依赖）
    └── works_collector.py             ← 54KB 作品深度采集器 V4（6行import改写）

12_radar/tools/
├── cookie_extractor.py                ← 已补抖音+知乎支持
└── cookie_extract_super_aigc.py       ← Super-AIGC 原版备份
```

### 🎯 三条路径，按需使用

| 路径 | 文件 | 门槛 | 采集内容 |
|:--:|------|------|------|
| **① 热搜** | `hotspot_collector.py` | 零依赖 `pip install requests` | 实时热搜 Top50 |
| **② 搜索** | `cookie_extractor.py` + `web_fetch` | `pip install rookiepy` + 浏览器登录 | 关键词搜索、用户基础信息 |
| **③ 深度** | `works_collector.py` | `pip install playwright playwright-stealth` + `playwright install chromium` | 用户全部作品 40+字段（视频+图文） |

---

### 路径 ② Cookie 提取（你现在就能做）

前提：在你的 Windows 台式机上用 **Chrome** 登录过 `douyin.com`。

```bash
# 在 WSL 或 Windows 终端：
pip install rookiepy
cd 12_radar
python3 -c "
from tools.cookie_extractor import extract_all
cookies = extract_all('chrome')
print(cookies.get('douyin', {}).get('cookie_string', '未找到抖音Cookie'))"
```

如果提取成功，把那一长串 cookie 字符串给我，我就可以 `web_fetch` 带 cookie 直接搜抖音。

> **如果 chrome 不在 WSL 里**：你只需要从 Windows Chrome 的 DevTools Console 粘贴 `document.cookie` 给我即可——这是路径 ② 的最简方案。

### 路径 ③ 深度作品采集（需要有 GUI）

`works_collector.py` 依赖 Playwright 启动真实 Chromium 窗口来走抖音的验证码和风控。这需要：
- **Windows 有显示器**：直接跑
- **WSL**: 配 X11 forwarding (`export DISPLAY=:0`) 或用 `channel="chrome"` 复用人已安装的 Windows Chrome

首次运行时弹窗扫码登录一次，之后登录态自动持久化到 `.browser-data/douyin/`，无需重复登录。

---

**现在你要哪种？** ①→我直接查热搜。②→给我 `document.cookie`。③→告诉我环境（Win/WSL），我指导安装 Playwright。
user: 你对用户的指引，要写进配置文件，清晰点
assistant: 好了。指南写在了：

> **`collectors/douyin/SETUP.md`** — 三步选路径 × 操作图解 × 常见问题

---

### 📋 你现在可以这样用了

**立刻能做的事（路径 ①）：**

> "查抖音热搜" → 我直接用 `hotspot_collector.py`

**2 分钟能做的事（路径 ②）：**

> 打开 Chrome → F12 → Console → `document.cookie` → 复制给我
> → 我就能搜抖音关键词、看用户主页

**15 分钟能做的事（路径 ③）：**

```bash
pip install playwright playwright-stealth && playwright install chromium
python3 -c "
from collectors.douyin.works_collector import DouyinAdvancedCollector4
c = DouyinAdvancedCollector4(headless=False)
c.login()  # 扫码一次，以后免登
"
```

---

现在要我先帮你查一下抖音热搜吗？还是你去拿 Cookie？

> 🐦 *SETUP.md 已写入 12_radar/collectors/douyin/SETUP.md · 2026-06-30*
